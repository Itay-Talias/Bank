class User {
    constructor(user_id, username, password, balance) {
        this.user_id = user_id;
        this.username = username;
        this.password = password;
        this.balance = balance;
    }
}

export default User;
